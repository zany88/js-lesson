/**
 * browser-sync --> 启动一个服务器
 * npm-run-all  --> 一起运行多个命令
 * sass --> 编译sass 成css
 * */

//
// "script":{
//     "build":"sass scss/main.scss css/style.css  --watch"
//     "serve":"browser-sync . --files \"css/*.css\""
//     "start":"run-p build serve"
// }



/**
 * Grunt  每次都会编译过后先写入临时文件中 基于临时文件
 * Gulp   每次都会编译过后写入内存中      基于内存
 * Fis    比较大而全
 * */
